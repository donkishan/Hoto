import { IsDivisionPipe } from './is-division-pipe';

describe('IsDivisionPipe', () => {
  it('create an instance', () => {
    const pipe = new IsDivisionPipe();
    expect(pipe).toBeTruthy();
  });
});
