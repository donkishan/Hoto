export interface LoginResponse {
  accessToken: string;
  refreshToken: string;
  userId: string;
  firstName: string;
  email: string;
  rolesId: string;
  divisionId: string;
  divisionCode: string;
  divisionName: string;
}
